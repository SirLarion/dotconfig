query QuestTemplatePreviewQuery($questId: ID!, $language: SUPPORTED_LOCALE) {
  questTemplates(filter: { _id_eq: $questId }) {
    _id
    preview(language: $language) {
      subject
      from {
        address
      }
      html
    }
  }
}
