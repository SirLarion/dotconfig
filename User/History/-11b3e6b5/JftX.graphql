mutation UpdateOrgDomainContextOrgTheme(
  $organizationId: ID!
  $input: DomainInput!
) {
  updateDomain(organizationId: $organizationId, domainInput: $input) {
    data {
      name
      context {
        organization {
          theme {
            mainColor
            logo {
              url
            }
          }
        }
      }
    }
    errors {
      message
    }
  }
}
