query OrgScimSettings($organizationId: ID!) {
  organizations(filter: { _id_eq: $organizationId }) {
    _id
    scim {
      authToken {
        createdAt
      }
    }
  }
  app {
    publicSettings {
      scimEndpoint
    }
  }
}
