query GetQuestTemplatePreview($tag: String!) {
  questTemplates(filter: { tag_eq: $tag }) {
    preview {
      html
      attachments {
        filename
      }
      from {
        address
        name
      }
      subject
    }
  }
}
