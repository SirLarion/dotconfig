mutation UserListAddRoleCommand(
  $organizationId: ID!
  $selectedUserIds: [ID!]!
) {
  addRoleForUsers(
    organizationId: $organizationId
    userIds: $selectedUserIds
    role: "${role}"
  ) {
    _id
    roles
  }
}
