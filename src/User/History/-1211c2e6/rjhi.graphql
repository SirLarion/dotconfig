mutation UserListAddRoleCommand(
  $organizationId: ID!
  $selectedUserIds: [ID!]!
) {
  addRoleForUsers(
    organizationId: $organizationId
    userIds: $selectedUserIds
    role: "${role}"
  ) {
    _id
    roles
  }
}

mutation UserListDemoteUserToNormalUser(
  $organizationId: ID!
  $selectedUserIds: [ID!]!
) {
  resetUsersRoles(organizationId: $organizationId, userIds: $selectedUserIds) {
    _id
    roles
  }
}
