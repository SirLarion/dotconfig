mutation sendQuestToOrganization(
  $organizationId: ID!
  $templateTag: String!
  $origin: QuestOrigin
) {
  sendQuestToOrganization(
    organizationId: $organizationId
    templateTag: $templateTag
    origin: $origin
  ) {
    _id
  }
}
