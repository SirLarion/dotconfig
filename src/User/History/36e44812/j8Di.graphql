query GetCurrentUserLocation {
  currentUser {
    _id
    profile {
      country
      geoLocation {
        latest {
          countryCode
        }
      }
    }
  }
}

mutation SetCurrentUserLocation($user: UserInput!) {
  updateUser(user: $user) {
    _id
    profile {
      country
    }
  }
}
